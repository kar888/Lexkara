# Redirects for Lexkara & Co
# Cloudflare Pages _redirects file
# Note: Cloudflare Pages automatically serves .html files without the extension
# so /about serves about.html, /practice serves practice.html, etc.
# Only add redirects for paths that don't match a file.

/privacy            /privacy-policy    301
